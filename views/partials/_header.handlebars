<header>
    <h1>{{title}}</h1>
    {{#if user}}
    <a href="/logout">Logout</a>
    <p>Welcome, {{user.name}}!</p>
    {{else}}
    <a href="/signup">Sign Up</a>
    <a href="/login">Log In</a>
    {{/if}}
</header>